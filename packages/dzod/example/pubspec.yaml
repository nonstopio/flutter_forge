name: dzod_example
description: Example Flutter app demonstrating dzod usage
publish_to: 'none'

version: 2.0.0

environment:
  sdk: '>=3.6.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dzod:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

resolution: workspace
