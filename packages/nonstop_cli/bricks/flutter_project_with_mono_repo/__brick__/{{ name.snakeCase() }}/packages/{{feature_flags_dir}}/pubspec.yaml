name: feature_flags
description: "Remote-config backed feature flags for {{name.titleCase()}}."
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.8.0
  flutter: ">=3.47.5"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.15.0
  firebase_remote_config: ^6.7.0
  core:
    path: ../core
  di:
    path: ../di

dev_dependencies:
  mocktail: ^1.0.4
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:

resolution: workspace
