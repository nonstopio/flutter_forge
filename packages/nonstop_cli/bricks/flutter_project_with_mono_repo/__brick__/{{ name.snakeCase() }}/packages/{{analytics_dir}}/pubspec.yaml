name: analytics
description: "Event tracking for {{name.titleCase()}} behind a swappable client interface."
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.8.0
  flutter: ">=3.47.5"

dependencies:
  flutter:
    sdk: flutter
  firebase_analytics: ^12.6.0
  core:
    path: ../core
  di:
    path: ../di

dev_dependencies:
  mocktail: ^1.0.4
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:

resolution: workspace
