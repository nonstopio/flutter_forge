name: network
description: "HTTP layer for {{name.titleCase()}}: Dio client, interceptors, typed responses and error mapping."
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.8.0
  flutter: ">=3.47.5"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  json_annotation: ^4.9.0
  talker_dio_logger: ^5.1.20
  talker_flutter: ^5.1.20
  core:
    path: ../core
  localization:
    path: ../localization
  di:
    path: ../di

dev_dependencies:
  mocktail: ^1.0.4
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.15
  json_serializable: ^6.9.5

flutter:

resolution: workspace
